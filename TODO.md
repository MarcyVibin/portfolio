### Features i want:

- [x] Get back to top button. There should be a button that lets people get back to the top. I want this to be next to the small dots on the far right that show the page im currently on.
- [x] I want everything you deem necessary that are applications that could use Logos get the logos from somewhere and add them for example the linked in logo etc (OG) or the github logo. for email i want you add a email symbol.
- [x] The buttons Github Linkedin and email have the font not completely centered its slightly to high. i don't want you to use a offset i want you to find and fix the issue.
- [x] The Skills section needs to be completely reworked. Just looks stupid and flat right now. make it more special.
- [x] The Projects section needs a few changes:
  - [x] I want it to show one project on the left and one on the right so that the boxes more closely resemble squares. the scroll bar currently is white which is ugly it should fit the theme.
  - [x] Rework the way the ReadMe is opened. Clicking a project should focus it and size it up to almost the full screen size. make sure this has a nice animation.
  - [x] Get rid of the ReadMe button and just make clicking the project something like a button.

---

### Open / needs your input

- **Skill levels are placeholders.** The meters in the Skills section are driven by
  `data-level` (1–5) on each `.skill-row` in `index.html`. I picked plausible values —
  adjust them to what's actually accurate.
- **`assets/resume.pdf` still missing**, so the Resume button hides itself at runtime.
  Drop the file in to make it appear.
- **`assets/profile.png` is 18.7 MB** — left as-is per your call. Worth compressing at
  some point; it's the slowest thing on the page.
